## Build Guide
1. Install vcpkg
2. Configure vcpkg directory in CMakePresets.json
3. ```cmake --preset default```
4. ```cmake --build --preset default```
5. ```.build/debug/DistributedDB```
6. To run tests, ```.build/debug/DistributedDB```
